{
    "$id": "https://hypar.io/schemas/elements/ElementTypes/StructuralFramingType.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "A container for properties common to a type of structural framing.",
    "title": "StructuralFramingType",
    "type": "object",
    "allOf": [{"$ref": "ElementType.json"}],
    "required": ["Profile", "Material"],
    "properties": {
        "ProfileId": {
            "description": "The id of the profile used by the structural framing type.",
            "type": "string",
            "format": "guid"
        },
        "MaterialId": {
            "description": "The id of the material used by the structural framing type.",
            "type": "string",
            "format": "guid"
        }
    },
    "additionalProperties": false
}